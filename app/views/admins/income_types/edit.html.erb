<div class="col-md-12">
  <%= form_with model: [:admins, @income_type], local: true do |f| %>
    <div>
      <span>収入カテゴリ</span>
      <span><%= f.text_field :type_name %></span>
      <span><%= f.submit "変更を保存", class:'btn btn-success' %></span>
    </div>
  <% end %>
</div>